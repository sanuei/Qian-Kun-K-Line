# GitHub Pages 设置指南

如果遇到 `Get Pages site failed` 错误，请按照以下步骤操作：

## 🔧 修复步骤

### 1. 启用 GitHub Pages

1. **进入 GitHub 仓库**：
   - 打开你的仓库页面
   - 点击右上角的 **Settings**

2. **配置 Pages**：
   - 在左侧菜单中找到 **Pages**
   - 在 **Source** 部分，选择 **GitHub Actions**
   - 点击 **Save**

### 2. 检查仓库权限

确保仓库不是私有的，或者如果是私有的，确保：
- 你有足够的权限
- GitHub Pages 支持私有仓库（需要 GitHub Pro/Team/Enterprise）

### 3. 创建 GitHub Pages 环境（如果需要）

如果 `github-pages` 环境不存在，GitHub 会在第一次部署时自动创建。如果遇到问题：

1. 进入仓库 **Settings → Environments**
2. 如果看到 **github-pages** 环境，点击它
3. 确保配置正确

### 4. 验证工作流文件

确保 `.github/workflows/deploy.yml` 文件存在且格式正确。

### 5. 手动触发部署

如果自动部署失败，可以手动触发：

1. 进入仓库的 **Actions** 标签页
2. 选择 **Deploy to GitHub Pages** 工作流
3. 点击 **Run workflow**
4. 选择分支（通常是 `main` 或 `master`）
5. 点击 **Run workflow**

## 🐛 常见错误及解决方案

### 错误 1: `HttpError: Not Found`

**原因**：GitHub Pages 未启用或环境不存在

**解决**：
1. 确保在 Settings → Pages 中选择了 "GitHub Actions"
2. 等待第一次部署完成后再检查

### 错误 2: `Get Pages site failed`

**原因**：Pages 站点未创建或配置不正确

**解决**：
1. 检查 Settings → Pages 配置
2. 确保 Source 设置为 "GitHub Actions"
3. 如果仍然失败，尝试先推送一个简单的提交触发工作流

### 错误 3: 权限不足

**原因**：仓库权限设置问题

**解决**：
1. 确保你是仓库的所有者或管理员
2. 检查仓库的 Actions 设置是否启用
3. 检查 Settings → Actions → General → Workflow permissions

## ✅ 验证部署

部署成功后：

1. 进入仓库的 **Actions** 标签页
2. 查看最新的工作流运行状态
3. 如果成功，会显示绿色的 ✓
4. 访问你的 GitHub Pages URL：`https://your-username.github.io/your-repo-name/`

## 📝 注意事项

- 首次部署可能需要几分钟
- 如果仓库名包含大写字母，URL 会自动转换为小写
- 确保 `vite.config.ts` 中的 `base` 配置与仓库名匹配

## 🔗 相关文档

- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [GitHub Actions 文档](https://docs.github.com/en/actions)

