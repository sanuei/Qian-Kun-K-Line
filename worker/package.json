{
  "name": "qiankun-gemini-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Workers proxy for Gemini API",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^4.55.0"
  }
}

